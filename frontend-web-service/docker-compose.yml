services:  
  web-interface-service:
    image: frontend-web-service
    build: .
    container_name: frontend-web-service
    restart: unless-stopped
    ports:
      - "8080:8080"
    env_file:
      ../.env
    environment:
      - SERVICE_NAME=frontend-web-service
      - SERVICE_PORT=8080
    networks:
      - ai-support-network
networks:
  ai-support-network:
    driver: bridge
      